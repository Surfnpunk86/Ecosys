# ── ANTHROPIC ──
ANTHROPIC_API_KEY=sk-ant-api03-PEGAR_AQUI_LA_CLAVE

# ── JWT ──
JWT_SECRET=CAMBIAR_POR_STRING_ALEATORIO_LARGO_MIN_64_CHARS
JWT_EXPIRES_IN=8h

# ── USUARIOS (usuario:contraseña_hasheada) ──
# Generar hash: node -e "const b=require('bcryptjs');console.log(b.hashSync('tuPassword',12))"
ADMIN_EMAIL=renzo@tudominio.com
ADMIN_PASSWORD_HASH=$2b$12$HASH_GENERADO_AQUI

# ── SERVER ──
PORT=3000
NODE_ENV=production

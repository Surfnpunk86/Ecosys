# Copia este archivo como ".env" para desarrollo local (NO subir .env a GitHub)
# En Render, esta variable se configura en el dashboard, no en un archivo.
ANTHROPIC_API_KEY=sk-ant-tu-api-key-aqui
PORT=3000
